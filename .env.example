DB_USER=postgres
DB_HOST=localhost
DB_NAME=your_database_name
DB_PASSWORD=your_password
DB_PORT=5432
PORT=3000

# Required for JWT signing in production (demo default exists in code — change this).
JWT_SECRET=use_a_long_random_string_in_production
